name: P12 Production Acceptance

on:
  push:
    branches:
      - ops/p12-preview-acceptance
    paths:
      - '.github/workflows/p12-production-acceptance.yml'

permissions:
  contents: read

env:
  VERCEL_CLI_VERSION: '59.5.0'
  VERCEL_TEAM_ID: team_qPw2lbnCHwH2czTLcopawsqH
  VERCEL_PROJECT_NAME: ziwei-ai-platform
  ZIWEI_AI_MODEL_PROFILES_FILE: config/ziwei-ai-model-profiles.json

jobs:
  production-acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout audited P12 branch
        uses: actions/checkout@v4
        with:
          ref: project/ziwei-ai-p12-production-guardrails

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install locked dependencies
        run: npm ci --ignore-scripts

      - name: Validate required secrets
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          ZIWEI_AI_BASE_URL: ${{ secrets.ZIWEI_AI_BASE_URL }}
          ZIWEI_AI_API_KEY: ${{ secrets.ZIWEI_AI_API_KEY }}
          ZIWEI_AI_MODEL: ${{ secrets.ZIWEI_AI_MODEL }}
        run: |
          set -euo pipefail
          test -n "$VERCEL_TOKEN"
          test -n "$ZIWEI_AI_BASE_URL"
          test -n "$ZIWEI_AI_API_KEY"
          test -n "$ZIWEI_AI_MODEL"

      - name: Link Vercel project
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: npx --yes vercel@${VERCEL_CLI_VERSION} link --yes --project "$VERCEL_PROJECT_NAME" --scope "$VERCEL_TEAM_ID" --token "$VERCEL_TOKEN"

      - name: Configure Production provider and P12 safety
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          ZIWEI_AI_BASE_URL: ${{ secrets.ZIWEI_AI_BASE_URL }}
          ZIWEI_AI_API_KEY: ${{ secrets.ZIWEI_AI_API_KEY }}
          ZIWEI_AI_MODEL: ${{ secrets.ZIWEI_AI_MODEL }}
          ZIWEI_AI_API_STYLE: ${{ secrets.ZIWEI_AI_API_STYLE }}
          ZIWEI_AI_TIMEOUT_MS: ${{ secrets.ZIWEI_AI_TIMEOUT_MS }}
          ZIWEI_AI_PROFILES_JSON: ${{ secrets.ZIWEI_AI_PROFILES_JSON }}
          ZIWEI_AI_DEFAULT_PROFILE: ${{ secrets.ZIWEI_AI_DEFAULT_PROFILE }}
        run: |
          set -euo pipefail
          DEFAULT_PROFILES_JSON=$(jq -c . "$ZIWEI_AI_MODEL_PROFILES_FILE")
          EFFECTIVE_PROFILES_JSON="${ZIWEI_AI_PROFILES_JSON:-$DEFAULT_PROFILES_JSON}"
          EFFECTIVE_DEFAULT_PROFILE="${ZIWEI_AI_DEFAULT_PROFILE:-default}"
          EFFECTIVE_TIMEOUT_MS="${ZIWEI_AI_TIMEOUT_MS:-60000}"
          PAYLOAD=$(jq -cn \
            --arg baseUrl "$ZIWEI_AI_BASE_URL" \
            --arg apiKey "$ZIWEI_AI_API_KEY" \
            --arg model "$ZIWEI_AI_MODEL" \
            --arg apiStyle "${ZIWEI_AI_API_STYLE:-auto}" \
            --arg timeoutMs "$EFFECTIVE_TIMEOUT_MS" \
            --arg profilesJson "$EFFECTIVE_PROFILES_JSON" \
            --arg defaultProfile "$EFFECTIVE_DEFAULT_PROFILE" \
            '[
              {key:"ZIWEI_AI_BASE_URL",value:$baseUrl,type:"plain",target:["production"]},
              {key:"ZIWEI_AI_API_KEY",value:$apiKey,type:"sensitive",target:["production"]},
              {key:"ZIWEI_AI_MODEL",value:$model,type:"plain",target:["production"]},
              {key:"ZIWEI_AI_API_STYLE",value:$apiStyle,type:"plain",target:["production"]},
              {key:"ZIWEI_AI_TIMEOUT_MS",value:$timeoutMs,type:"plain",target:["production"]},
              {key:"ZIWEI_AI_PROFILES_JSON",value:$profilesJson,type:"sensitive",target:["production"]},
              {key:"ZIWEI_AI_DEFAULT_PROFILE",value:$defaultProfile,type:"plain",target:["production"]},
              {key:"ZIWEI_AI_INTERPRET_ENABLED",value:"true",type:"plain",target:["production"]},
              {key:"ZIWEI_AI_MAX_INFLIGHT",value:"2",type:"plain",target:["production"]},
              {key:"ZIWEI_AI_MAX_PROVIDER_CALLS",value:"2",type:"plain",target:["production"]},
              {key:"ZIWEI_AI_ALLOWED_ORIGINS",value:"https://ziwei-ai-platform.vercel.app",type:"plain",target:["production"]}
            ]')
          curl --fail-with-body --silent --show-error \
            --request POST \
            --url "https://api.vercel.com/v10/projects/${VERCEL_PROJECT_NAME}/env?teamId=${VERCEL_TEAM_ID}&upsert=true" \
            --header "Authorization: Bearer ${VERCEL_TOKEN}" \
            --header 'Content-Type: application/json' \
            --data "$PAYLOAD" \
            --output /tmp/p12-production-env.json
          jq -e '(.failed // []) | length == 0' /tmp/p12-production-env.json >/dev/null

      - name: Pull Production environment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: npx --yes vercel@${VERCEL_CLI_VERSION} pull --yes --environment=production --scope "$VERCEL_TEAM_ID" --token "$VERCEL_TOKEN"

      - name: Build Production output
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: npx --yes vercel@${VERCEL_CLI_VERSION} build --prod --scope "$VERCEL_TEAM_ID" --token "$VERCEL_TOKEN"

      - name: Deploy audited P12 to Production
        id: deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -euo pipefail
          URL=$(npx --yes vercel@${VERCEL_CLI_VERSION} deploy --prebuilt --prod --scope "$VERCEL_TEAM_ID" --token "$VERCEL_TOKEN")
          echo "deployment_url=$URL" >> "$GITHUB_OUTPUT"
          echo "Production deployed: $URL"

      - name: Validate P12 Production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment_url }}
        run: |
          set -euo pipefail
          CLI="npx --yes vercel@${VERCEL_CLI_VERSION}"

          $CLI curl /api/health --deployment "$DEPLOYMENT_URL" > /tmp/health.json
          jq -e '.status == "ok" and .interpretSafety.state == "configured" and .interpretSafety.enabled == true and .interpretSafety.maxInflight == 2 and .interpretSafety.maxProviderCalls == 2 and .interpretSafety.allowedOriginCount == 1' /tmp/health.json >/dev/null

          $CLI curl /api/ready --deployment "$DEPLOYMENT_URL" > /tmp/ready.json
          jq -e '.ready == true and .aiProvider.state == "configured" and .aiProvider.profileCount >= 2 and .interpretSafety.enabled == true' /tmp/ready.json >/dev/null

          $CLI curl /api/ziwei-ai/models --deployment "$DEPLOYMENT_URL" > /tmp/models.json
          jq -e '.configured == true and (.profiles | length >= 2)' /tmp/models.json >/dev/null
          jq -e 'any(.profiles[]; .id == "qwen-plus" and .model == "qwen3.7-plus" and .apiStyle == "messages")' /tmp/models.json >/dev/null

          bad_origin_code=$($CLI curl /api/ziwei-ai/interpret --deployment "$DEPLOYMENT_URL" -- \
            --request POST --header 'Content-Type: application/json' --header 'Origin: https://evil.example' \
            --data '{}' --silent --output /tmp/bad-origin.json --write-out '%{http_code}')
          test "$bad_origin_code" = '403'
          jq -e '.error == "origin-not-allowed"' /tmp/bad-origin.json >/dev/null

          $CLI curl /api/ziwei-ai/interpret --deployment "$DEPLOYMENT_URL" -- \
            --request POST --header 'Content-Type: application/json' --max-time 120 \
            --dump-header /tmp/interpret.headers \
            --data '{"input":{"calendarType":"solar","date":"2000-8-16","hourIndex":2,"gender":"female"},"topic":"overview","question":"请仅依据命盘事实、格局证据与检索到的古籍证据做一段简要综合解读。"}' > /tmp/interpret.json
          jq -e '.report.sections | type == "array" and length > 0' /tmp/interpret.json >/dev/null
          jq -e '.critic | type == "object" and .critic.passed == true' /tmp/interpret.json >/dev/null
          jq -e '.modelProfile | type == "object"' /tmp/interpret.json >/dev/null
          grep -qi '^x-request-id:' /tmp/interpret.headers
          grep -qi '^cache-control: no-store' /tmp/interpret.headers

          echo '## P12 Production acceptance' >> "$GITHUB_STEP_SUMMARY"
          echo "- Deployment: $DEPLOYMENT_URL" >> "$GITHUB_STEP_SUMMARY"
          echo '- Public alias: https://ziwei-ai-platform.vercel.app' >> "$GITHUB_STEP_SUMMARY"
          echo '- Malicious browser Origin: HTTP 403' >> "$GITHUB_STEP_SUMMARY"
          jq -r '"- Safety: enabled=`\(.interpretSafety.enabled)` inflight=`\(.interpretSafety.maxInflight)` calls/request=`\(.interpretSafety.maxProviderCalls)` origins=`\(.interpretSafety.allowedOriginCount)`"' /tmp/ready.json >> "$GITHUB_STEP_SUMMARY"
          jq -r '"- AI: profile=`\(.modelProfile.id)` critic=`\(.critic.passed)` revised=`\(.revised)` sections=`\(.report.sections|length)`"' /tmp/interpret.json >> "$GITHUB_STEP_SUMMARY"
